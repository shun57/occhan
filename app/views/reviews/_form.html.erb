<%= form_with(model: [problem, review]) do |form| %>
  <% if review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this comment from being saved:</h2>
      <ul>
      <% review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :レビュー内容 %>
    <%= form.text_area :review_content %>
  </div>

  <div class="actions">
    <%= form.submit "投稿", class:"btn btn-success btn-sm" %>
  </div>
<% end %>
